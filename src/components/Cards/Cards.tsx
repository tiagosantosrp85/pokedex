import React from 'react';
import { Container } from './styles';

const Cards: React.FC = () => {
  return (
    <Container>
      <button>
        <p>Search</p>
      </button>
    </Container>
  );
};

export default Cards;
